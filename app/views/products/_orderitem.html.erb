<%= form_for @product.product_carts.build ,remote: true do |f|%>
  <%= f.hidden_field :product_id, value: @product.id %>
  <%= f.hidden_field :quantity, value: 1 %>
  <%= f.hidden_field :total_product, value: @product.price %>
  <%= f.submit "Add cart", class:"btn btn-success" %>
<% end %>